var csv_cj = require('..');

csv_cj({
    input: __dirname + '/test.csv',
    output: null
  }, function(err, result) {
    if(err) {
      console.error(err);
    }else {
      console.log(result);
    }
  
  }
  );
